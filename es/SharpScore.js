import React from 'react';
import { Icon } from './Icon';
import { zd, bez } from './fragments';
export var SharpScore =
/*#__PURE__*/
function SharpScore(props) {
  return React.createElement(Icon, props, React.createElement("path", {
    d: zd + "m-9 2" + bez
  }));
};