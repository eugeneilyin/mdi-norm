import React from 'react';
import { Icon } from './Icon';
import { we, beq } from './fragments';
export var FilledDeleteForever =
/*#__PURE__*/
function FilledDeleteForever(props) {
  return React.createElement(Icon, props, React.createElement("path", {
    d: we + "V7H6zm2.46-7.12" + beq
  }));
};