import React from 'react'
import { Icon } from './Icon'
import { ce, dd, fw } from './fragments'

export const FilledHdrWeak = /*#__PURE__*/ props => <Icon {...props}>
  <path d={"M5 8" + ce + "m12-2" + fw + "m0 10" + dd}/>
</Icon>
