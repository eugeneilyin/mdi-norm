import React from 'react'
import { Icon } from './Icon'
import { k, x, cf, vw } from './fragments'

export const RoundCallToAction = /*#__PURE__*/ props => <Icon {...props}>
  <path d={cf + "m-1 16H4" + k + "v-1" + vw + "v1" + x + "z"}/>
</Icon>
