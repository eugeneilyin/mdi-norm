import React from 'react'
import { Icon } from './Icon'
import { r, dd, fw } from './fragments'

export const OutlineBrightnessLow = /*#__PURE__*/ props => <Icon {...props}>
  <path d={r + "M12 6" + fw + "m0 10" + dd}/>
</Icon>
