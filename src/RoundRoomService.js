import React from 'react'
import { Icon } from './Icon'
import { dk, el, sa } from './fragments'

export const RoundRoomService = /*#__PURE__*/ props => <Icon {...props}>
  <path d={"M3 17h18" + dk + "H3" + el + "zm10.84-9.21" + sa}/>
</Icon>
